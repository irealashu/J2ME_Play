<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:colorBackground">

    <!-- Games List -->
    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:paddingTop="8dp"
        android:paddingBottom="88dp"
        android:scrollbarStyle="outsideOverlay" />

    <!-- Modern Rich Empty State -->
    <LinearLayout
        android:id="@android:id/empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp"
        android:visibility="gone"
        tools:visibility="visible">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:contentDescription="@null"
            android:src="@drawable/ic_games_empty" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:text="@string/empty_games_title"
            android:textColor="?attr/textColorPrimary"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:maxWidth="320dp"
            android:text="@string/empty_games_subtitle"
            android:textColor="?attr/textColorSecondary"
            android:textSize="14sp" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:gravity="center"
            android:orientation="vertical">

            <Button
                android:id="@+id/btn_select_jar"
                style="@style/ButtonStyle"
                android:layout_width="220dp"
                android:layout_height="48dp"
                android:text="@string/btn_select_jar"
                android:textAllCaps="false"
                android:textColor="@color/btn_text"
                android:textSize="15sp" />

            <Button
                android:id="@+id/btn_load_demos"
                style="@style/ButtonStyle"
                android:layout_width="220dp"
                android:layout_height="48dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/bg_config_card"
                android:text="@string/btn_load_demo_games"
                android:textAllCaps="false"
                android:textColor="@color/accent"
                android:textSize="15sp" />

        </LinearLayout>

    </LinearLayout>

    <!-- Floating Action Button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/floating_action_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_margin="20dp"
        android:contentDescription="@string/install_game"
        app:backgroundTint="@color/accent"
        app:elevation="6dp"
        app:fabSize="normal"
        app:rippleColor="@color/primary_dark"
        app:srcCompat="@drawable/ic_add_white"
        app:tint="#000000" />

</FrameLayout>
